import { PositiveNegativeDirective } from './positive-negative.directive';

describe('PositiveNegativeDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveNegativeDirective();
    expect(directive).toBeTruthy();
  });
});
